input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

filter {
  if [logger_name] =~ /org\.springframework/ {
    mutate {
      add_tag => ["spring"]
    }
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "drugstore-logs-%{+YYYY.MM.dd}"
  }
  stdout {
    codec => rubydebug
  }
}